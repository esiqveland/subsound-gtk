# TODO

## RELEASE v1
- [x] flatpak env: fix and verify handling of offline artwork after sync at the very least
- [x] flatpak env: fix issue with offline mode triggering before onboarding is completed
- [x] flatpak: need a svg icon for the app

## General

Playlist:
    - next level: "keep all songs available offline"
    - Find a way to add a context menu to the rows in the playlist page + button to remove from playlist
    - what about enable multi-select, then a delete action button becomes visible on the top of the playlist?

GSongInfo:
 - [ ] extend is-playing to a richer playing state (buffering, paused ?)

- downloads:
  - need to store the format and bitrate we downloaded mp3/opus/flac
  - need to show the format/quality and file size in the playlist page
  - delete button?
  - potentially add a way to check the status of downloaded files on disk
  - fix the downloaded count in the playlist sidebar

- List an overview of downloaded songs and cached content from the server on the configuration popover in the upper right corner
    - Add a button to sync data from the server

playqueue:
- database as a backing store for the PlayQueue, so we can persist the queue across restarts.
